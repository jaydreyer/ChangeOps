export default function LoadingPolicyComparison() {
  return (
    <main className="entry-shell">
      <section className="entry-card" aria-live="polite" aria-busy="true">
        <p className="eyebrow">Loading immutable comparison</p>
        <h1>Resolving accepted policy lineage</h1>
        <p>ChangeOps is loading persisted semantic differences and provenance.</p>
      </section>
    </main>
  );
}
