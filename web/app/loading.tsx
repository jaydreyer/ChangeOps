export default function Loading() {
  return (
    <main className="entry-shell">
      <section className="entry-card" aria-live="polite" aria-busy="true">
        <p className="eyebrow">Loading authoritative records</p>
        <h1>Opening policy analysis</h1>
        <p>The application is retrieving persisted policy and workflow state.</p>
      </section>
    </main>
  );
}
